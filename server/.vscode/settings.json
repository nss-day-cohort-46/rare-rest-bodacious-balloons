{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "rare-rest-api",
            "database": "${workspaceFolder:server}/db.sqlite3"
        }
    ]
}